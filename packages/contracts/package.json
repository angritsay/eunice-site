{
  "name": "@eunice/contracts",
  "private": true,
  "type": "module",
  "description": "The wire format shared by the site and the services: the form registry, HTTP schemas, event schemas",
  "exports": {
    ".": "./src/index.ts",
    "./forms": "./src/forms/index.ts",
    "./http": "./src/http/index.ts",
    "./events": "./src/events/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json",
    "asyncapi": "node scripts/asyncapi.ts"
  },
  "dependencies": {
    "zod": "4.6.5"
  }
}
